<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <title>{% block title %}CRM CORE // Enterprise Intelligence{% endblock %}</title>
        <link rel="preconnect" href="https://fonts.googleapis.com">
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
        <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&family=Outfit:wght@400;500;600;700;800&display=swap" rel="stylesheet">
        <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
        {% block stylesheets %}{% endblock %}
        {% block javascripts %}
            {% block importmap %}{{ importmap('app') }}{% endblock %}
        {% endblock %}
    </head>
    <body class="d-flex flex-column min-vh-100">
        <nav class="navbar navbar-expand-lg">
            <div class="container">
                <a class="navbar-brand d-flex align-items-center" href="{{ path('app_campaign_index') }}">
                    <i class="bi bi-layers-half me-2 text-primary"></i>
                    <span>CRM<span class="text-primary">CORE</span></span>
                </a>
                <button class="navbar-toggler border-0 shadow-none" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarNav">
                    <div class="navbar-nav ms-auto align-items-center">
                        <a class="nav-link mx-2" href="{{ path('app_campaign_index') }}">Initiatives</a>
                        <a class="nav-link mx-2" href="#">Strategic Donors</a>
                        <a class="nav-link ms-3 btn btn-primary text-white px-4" href="#">Executive Dashboard</a>
                    </div>
                </div>
            </div>
        </nav>

        <main class="container py-5 flex-grow-1">
            {% if app.session and app.session.started %}
                {% for label, messages in app.flashes %}
                    {% for message in messages %}
                        <div class="alert alert-{{ label }} alert-dismissible fade show border-0 shadow-lg mb-5 rounded-3 px-4 py-3" role="alert">
                            <div class="d-flex align-items-center">
                                <i class="bi bi-check-circle-fill me-3 fs-5"></i>
                                <span class="fw-medium">{{ message }}</span>
                            </div>
                            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                        </div>
                    {% endfor %}
                {% endfor %}
            {% endif %}

            {% block body %}{% endblock %}
        </main>

        <footer class="mt-auto">
            <div class="container text-center">
                <div class="row pt-5 pb-4 border-top">
                    <div class="col-12 mb-4">
                        <div class="navbar-brand mb-2">
                            <i class="bi bi-layers-half text-primary"></i>
                            CRM<span class="text-primary">CORE</span>
                        </div>
                        <p class="text-muted small">The modern standard for non-profit intelligence and donor management.</p>
                    </div>
                    <div class="col-12">
                        <div class="d-flex justify-content-center gap-4 mb-4">
                            <a href="#" class="text-muted text-decoration-none small">Platform</a>
                            <a href="#" class="text-muted text-decoration-none small">Security</a>
                            <a href="#" class="text-muted text-decoration-none small">Intelligence</a>
                            <a href="#" class="text-muted text-decoration-none small">Support</a>
                        </div>
                    </div>
                    <div class="col-12 d-flex justify-content-between align-items-center pt-4 border-top">
                        <span class="text-muted small fw-medium">&copy; {{ "now"|date("Y") }} CRM Core Intelligence.</span>
                        <div class="d-flex align-items-center small text-muted">
                            <span class="badge bg-success bg-opacity-10 text-success border-success border-opacity-25 py-1 px-3 me-2">LIVE</span>
                            <span class="fw-semibold">V2.4.0</span>
                        </div>
                    </div>
                </div>
            </div>
        </footer>
    </body>
</html>
